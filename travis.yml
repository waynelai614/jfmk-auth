language: ruby

rvm:
  - 2.4.0p0

services:
  - docker

before_install:
  - docker -v
  - docker-compose -v

install:
  - docker-compose build

script:
  - docker-compose up -d
  - docker-compose exec test rails test
